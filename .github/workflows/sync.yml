name: Google Drive Auto Sync

on:
  schedule:
    - cron: '*/10 * * * *'
  workflow_dispatch:
  repository_dispatch:
    types: [sync-trigger]

concurrency:
  group: sync-gdrive
  cancel-in-progress: false

jobs:
  sync:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Install rclone
        run: |
          curl -s https://rclone.org/install.sh | sudo bash
          rclone version | head -1

      - name: Setup rclone config
        run: |
          mkdir -p ~/.config/rclone
          echo '${{ secrets.RCLONE_CONFIG }}' > ~/.config/rclone/rclone.conf

      - name: Sync folders
        id: sync
        continue-on-error: true
        env:
          SYNC_PAIRS: ${{ secrets.SYNC_FOLDERS }}
        run: |
          echo "0" > /tmp/filecount.txt
          echo "0" > /tmp/totalsize.txt
          > /tmp/notify.txt
          
          TOTAL=0
          TOTALSIZE=0
          
          PAIR1=$(echo "$SYNC_PAIRS" | cut -d',' -f1)
          PAIR2=$(echo "$SYNC_PAIRS" | cut -d',' -f2)
          
          # Sync pair 1
          if [ -n "$PAIR1" ]; then
            SRC1=$(echo "$PAIR1" | cut -d':' -f1)
            DST1=$(echo "$PAIR1" | cut -d':' -f2)
            echo "Pair 1: $SRC1 -> $DST1"
            
            rclone copy "gdrive:${SRC1}" "gdrive:${DST1}" --exclude "*.tmp" --exclude "Thumbs.db" -v --stats 1s 2>&1 | tee /tmp/log1.txt || true
            
            C1=$(grep -c ": Copied" /tmp/log1.txt 2>/dev/null || echo "0")
            echo "Pair 1 copied: $C1"
            
            # Extract size (e.g., "Transferred: 123.456 MiB")
            SIZE1=$(grep -oE 'Transferred:[^,]+' /tmp/log1.txt | tail -1 | sed 's/Transferred://' | xargs || echo "0 B")
            
            if [ "$C1" -gt 0 ]; then
              echo "📦 $SRC1 → $DST1:" >> /tmp/notify.txt
              
              # Get file list first
              grep ": Copied" /tmp/log1.txt | sed -E 's/.*INFO *: *(.+): Copied.*/\1/' | head -15 > /tmp/files1.txt
              FILECOUNT1=$(wc -l < /tmp/files1.txt)
              CURRENT=0
              
              while IFS= read -r filepath; do
                ((CURRENT++))
                ext="${filepath##*.}"
                ext="${ext,,}"
                case "$ext" in
                  pdf) icon="📕" ;;
                  doc|docx) icon="📘" ;;
                  xls|xlsx) icon="📗" ;;
                  ppt|pptx) icon="📙" ;;
                  jpg|jpeg|png|gif|webp) icon="🖼️" ;;
                  mp4|avi|mkv|mov) icon="🎬" ;;
                  mp3|wav|flac) icon="🎵" ;;
                  zip|rar|7z) icon="📦" ;;
                  exe|bat|sh) icon="⚙️" ;;
                  *) icon="📄" ;;
                esac
                
                # Last item uses └── 
                if [ "$CURRENT" -eq "$FILECOUNT1" ]; then
                  prefix="└──"
                else
                  prefix="├──"
                fi
                
                if [[ "$filepath" == *"/"* ]]; then
                  echo "$prefix 📂 $icon $filepath" >> /tmp/notify.txt
                else
                  echo "$prefix $icon $filepath" >> /tmp/notify.txt
                fi
              done < /tmp/files1.txt
            fi
            TOTAL=$((TOTAL + C1))
          fi
          
          # Sync pair 2
          if [ -n "$PAIR2" ] && [ "$PAIR2" != "$PAIR1" ]; then
            SRC2=$(echo "$PAIR2" | cut -d':' -f1)
            DST2=$(echo "$PAIR2" | cut -d':' -f2)
            echo "Pair 2: $SRC2 -> $DST2"
            
            rclone copy "gdrive:${SRC2}" "gdrive:${DST2}" --exclude "*.tmp" --exclude "Thumbs.db" -v --stats 1s 2>&1 | tee /tmp/log2.txt || true
            
            C2=$(grep -c ": Copied" /tmp/log2.txt 2>/dev/null || echo "0")
            echo "Pair 2 copied: $C2"
            
            SIZE2=$(grep -oE 'Transferred:[^,]+' /tmp/log2.txt | tail -1 | sed 's/Transferred://' | xargs || echo "0 B")
            
            if [ "$C2" -gt 0 ]; then
              echo "" >> /tmp/notify.txt
              echo "📦 $SRC2 → $DST2:" >> /tmp/notify.txt
              
              grep ": Copied" /tmp/log2.txt | sed -E 's/.*INFO *: *(.+): Copied.*/\1/' | head -15 > /tmp/files2.txt
              FILECOUNT2=$(wc -l < /tmp/files2.txt)
              CURRENT=0
              
              while IFS= read -r filepath; do
                ((CURRENT++))
                ext="${filepath##*.}"
                ext="${ext,,}"
                case "$ext" in
                  pdf) icon="📕" ;;
                  doc|docx) icon="📘" ;;
                  xls|xlsx) icon="📗" ;;
                  ppt|pptx) icon="📙" ;;
                  jpg|jpeg|png|gif|webp) icon="🖼️" ;;
                  mp4|avi|mkv|mov) icon="🎬" ;;
                  mp3|wav|flac) icon="🎵" ;;
                  zip|rar|7z) icon="📦" ;;
                  exe|bat|sh) icon="⚙️" ;;
                  *) icon="📄" ;;
                esac
                
                if [ "$CURRENT" -eq "$FILECOUNT2" ]; then
                  prefix="└──"
                else
                  prefix="├──"
                fi
                
                if [[ "$filepath" == *"/"* ]]; then
                  echo "$prefix 📂 $icon $filepath" >> /tmp/notify.txt
                else
                  echo "$prefix $icon $filepath" >> /tmp/notify.txt
                fi
              done < /tmp/files2.txt
            fi
            TOTAL=$((TOTAL + C2))
          fi
          
          echo "=== TOTAL: $TOTAL ==="
          echo "$TOTAL" > /tmp/filecount.txt
          
          # Combine sizes for display
          echo "${SIZE1:-0 B} + ${SIZE2:-0 B}" > /tmp/totalsize.txt
          
          cat /tmp/notify.txt

      - name: Update state.json
        if: always()
        run: |
          FILES=$(cat /tmp/filecount.txt 2>/dev/null || echo "0")
          FILES=${FILES:-0}
          NOW=$(TZ='Asia/Ho_Chi_Minh' date '+%Y-%m-%d %H:%M:%S')
          DETAILS=$(cat /tmp/notify.txt 2>/dev/null | tr '\n' '|' | head -c 500 || echo "")
          
          if [ ! -f state.json ]; then
            echo '{"stats":{"totalSyncs":0,"totalFiles":0,"lastSync":""},"history":[]}' > state.json
          fi
          
          jq --arg now "$NOW" --argjson files "$FILES" --arg details "$DETAILS" \
            '.stats.totalSyncs += 1 | .stats.totalFiles += $files | .stats.lastSync = $now | .history = ([{"time": $now, "files": $files, "details": $details}] + .history)[:50]' \
            state.json > tmp.json && mv tmp.json state.json

      - name: Commit state
        if: always()
        run: |
          git config user.name "github-actions"
          git config user.email "actions@github.com"
          git add state.json
          git diff --staged --quiet || git commit -m "Sync $(TZ='Asia/Ho_Chi_Minh' date '+%H:%M')"
          git push || true

      - name: Telegram notification
        if: always()
        env:
          BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
        run: |
          FILES=$(cat /tmp/filecount.txt 2>/dev/null || echo "0")
          FILES=${FILES:-0}
          
          if [ "$FILES" -gt 0 ]; then
            NOW=$(TZ='Asia/Ho_Chi_Minh' date '+%H:%M:%S')
            DETAILS=$(cat /tmp/notify.txt 2>/dev/null || echo "")
            SIZE=$(cat /tmp/totalsize.txt 2>/dev/null || echo "")
            
            GREETINGS=("🔔 Có file mới nè sếp ơi!" "🎉 Oni-chan, có file mới!" "📢 Thầy ơi có tài liệu mới!" "🚀 Đại ca, hàng về rồi nè!" "✨ Boss, file mới đã sync!")
            RANDOM_IDX=$((RANDOM % 5))
            
            # Build message with size stats
            MSG="${GREETINGS[$RANDOM_IDX]}"$'\n'"🕒 $NOW"$'\n'"📁 $FILES file(s)"
            if [ -n "$SIZE" ] && [ "$SIZE" != "0 B + 0 B" ] && [ "$SIZE" != " + " ]; then
              MSG="$MSG | 📊 $SIZE"
            fi
            MSG="$MSG"$'\n\n'"$DETAILS"$'\n'"───────────"$'\n'"💡 /sync • /status"
            
            curl -s -X POST "https://api.telegram.org/bot${BOT_TOKEN}/sendMessage" -d "chat_id=${CHAT_ID}" -d "text=${MSG}"
          else
            echo "No files, skip notification"
          fi
